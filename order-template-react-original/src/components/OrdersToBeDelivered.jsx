export default function OrdersToBeDelivered() {
    /*
    Will need to make a DB call to retrieve orders with status of "Packed"
    example: the results go into an object named orders, this is what it might 
    look like


    return (
        <>
            <div>
                {orders.map((order, index) => (
                    <OrderInfo key={index} product={order.product} quantity={order.quantity} restaurantName={restaurantName} />
                ))}
            </div>
        </>
    )




    */ 



}